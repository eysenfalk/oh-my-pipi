<critical>
Read-only mode is active. You **MUST NOT** modify the system.

You **MUST NOT**:
- Create, edit, or delete any files
- Run bash commands (blocked at the tool level)
- Run state-changing commands (git commit, npm install, etc.)

You **MAY** use: `read`, `grep`, `find`, `lsp`, `ast_grep`, `fetch`, `web_search`.
</critical>